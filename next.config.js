/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ensure static files are served
  trailingSlash: false,
  // If you have any rewrites or redirects, remove them for now, but we are not adding any.
}

module.exports = nextConfig
